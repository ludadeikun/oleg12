установка git

https://git-scm.com/downloads           
----------------------------------------------------------------------------------------

Инициализация git (создание локального репозитория)


git init

----------------------------------------------------------------------------------------

Базовые настройки (должны совпадать с данными на github)

git config --global user.name "OlegKozintsev" - имя пользователя
git config --global user.email on.kozintsev@gmail.com  - мэил пользователя
git config --list (показывает файл конфигурации проекта в git)
git help "команда" (показывает справку по выбранной команде например git help init )

Создание репозитория на github
-----------------------------------------------------------------------------------------

https://github.com

-----------------------------------------------------------------------------------------

Загрузка файлов через git

git remote add origin https://github.com/OlegKozintsev/distfile.git
git push -u origin master


также можно просто загрузить файлы напрямую через web интерфейс github

--------------------------------------ИЛИ------------------------------------------------

Подключиться к работе над проектом

создаем папку проекта

git init

fork   - создает копию репозитория в вашем аккаунте гитхаб (на github)

git clone + url репозитория  - сделать локальную копию на компьютере

сd <папка склонированного репозитория>

-----------------------------------------------------------------------------------------

получение актуальной версии проекта

git fetch   - получить с удаленного репозитория последние версии файлов

git pull    - выгрузить полученные файлы в папку с копией проекта

создать на github запрос на добавление изменений (pull requests)


-----------------------------------------------------------------------------------------
Базовые операции c github

git status  (текущее состояние файлов с изменениями)

git add .     (добавляет изменненые файлы в локальный репозиторий git)

git commit  -m"add file"   (фиксирует слепок/снимок файлов на текущий момент)

git push   (синхронизирует/отправляет файлы на удаленный репозиторий)


-----------------------------------------------------------------------------------------
Командная работа

git branch -v (показывает список веток)

git branch 

git checkout имяветки  (переключение на конкретную ветку)

git checkout -b branch3 (создание и переход в созданную ветку)

---------------------------------------------------------------------------------------

возврат локального репозитория до состояния опредленного коммита

git log
q - выход из просмотра log
git checkout индекс комита  название файла

---------------------------------------------------------------------------------------

возврат репозитория до указанного комита (для github)
git log - просматриваем все комиты и находим нужный
q - выход из просмотра log
$ git checkout -- <файл>
$ git push -f  синхронизация изменений в github несмотря на версию

---------------------------------------------------------------------------------------

Ссылка на сайт https://olegkozintsev.github.io/testfed/app/

---------------------------------------------------------------------------------------

node.js

npm i gulp -g

npm i gulp --save-dev

npm init



npm i gulp-sass gulp-sass-glob browser-sync gulp-concat gulp-uglify gulp-cssnano gulp-rename del  gulp-cache gulp-autoprefixer  gulp-flatten gulp-sourcemaps   --save-dev